services:
  editorial-estate:
    build:
      context: .
      dockerfile: Dockerfile
    image: editorial-estate:latest
    container_name: editorial-estate
    ports:
      - "8080:8787"
    restart: unless-stopped
